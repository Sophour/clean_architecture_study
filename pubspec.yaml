name: tddcleanarchitecturecourse
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  # Service locator
  get_it: ^2.0.1
  flutter_bloc: ^0.21.0
  # Value equality
  equatable: ^0.4.0
  # Functional programming thingies
  dartz: ^0.8.6
  # Remote API. Platform independent!
  data_connection_checker: ^0.3.4
  http: ^0.12.0+2
  # Local cache
  shared_preferences: ^0.5.3+4


dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.0
  pedantic: ^1.7.0


flutter:

  uses-material-design: true
